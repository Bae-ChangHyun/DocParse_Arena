# ===========================================
# DocParse Arena - Environment Configuration
# ===========================================

# Admin authentication (leave empty to disable admin protection)
ADMIN_PASSWORD=

# --- API Keys ---
# At least one API key is required for OCR to work.
# Only configure the providers you want to use.

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# OpenAI (GPT-4o, etc.)
OPENAI_API_KEY=

# Google (Gemini)
GOOGLE_API_KEY=

# Mistral
MISTRAL_API_KEY=

# --- Ollama (local models) ---
# If using Ollama on the host machine, use host.docker.internal
OLLAMA_BASE_URL=http://host.docker.internal:11434

# --- Database ---
# SQLite database path (auto-created, persisted via Docker volume)
DATABASE_URL=sqlite+aiosqlite:///./data/ocr_arena.db

# --- CORS ---
# Allowed origins for frontend access (JSON array format)
# Adjust for your domain in production
CORS_ORIGINS=["http://localhost:3000"]
